name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
env:
  myvar: "This is enviorenment variable"
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - run: echo "test"
#    - uses: actions/checkout@v3
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
